apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  ports:
  - name: "80"
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: backend-deployment
  type: LoadBalancer
